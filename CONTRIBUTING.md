# 开发与 Git 准则

## 目标与协作

以建立理解为先，以可运行的结果检验理解。Agent 负责实现、验证和逐步讲解，项目所有者参与重要设计选择与审查。

每次只推进一个清晰能力。讲解应使用具体输入、状态变化和输出，说明设计理由以及测试能证明什么。新术语首次出现时解释含义。

普通实现细节在已确定范围内自主完成；影响架构边界、外部行为或学习路线的新选择先讨论。已经明确的授权不重复询问。

## 开发流程

所有开发遵循 INTENTION → SPEC → IMPLE → VERIFY，工作量小时可在同一份任务记录中简写，避免机械填表。

### INTENTION：为什么现在做

说明当前问题、谁需要、为什么现在需要、为何不能推迟，以及本次不解决什么。需求可以是运行需求，也可以是可通过实验回答的具体学习问题。

没有当前需求的能力只记为未来考虑。阶段表是默认路线，强制顺序应来自真实依赖。

### SPEC：外部行为是什么

说明行为、接口、状态、数据格式、适用的地址或 ABI、错误处理、不变量、例子和验证方法。解释 WHAT，避免把实现细节写成不可更改的要求。

ISA、CSR、异常和特权行为必须查官方规范。首次实现前记录采用的规范版本、章节及链接；尚未实现的能力不得标为支持。

### IMPLE：按规格实现

先列出要修改的文件、影响的模块、最小实现和验证计划，再写代码。

ISA 行为遵循 Spec → Test → Implementation：先写用例并观察预期失败，再实现并运行用例。测试先写不意味着要单独提交已知失败的测试；正常提交应包含配套实现与测试。

### VERIFY：证明并解释

逐条对应规格，记录实际执行的检查及结果，检查架构边界、相关回归和剩余限制。区分已经验证、尚未验证和推断。

说明本阶段回答了什么问题，并以一个实际例子讲解执行过程。Agent 不得把“代码完成”直接写成“项目所有者已经理解”；需要对方判断的审查保持待审状态。

如果实现揭示规格错误，先说明证据并修订相关决定，再继续实现，禁止悄悄迁就现有代码。

## 技术约束

- Host 核心使用 C17，先本机运行；Guest 使用 C 和必要的 RISC-V 汇编。
- 使用明确的定宽数据与字节序；不能依赖 Host 的有符号溢出、未对齐指针访问等行为模拟 Guest。
- CPU 经 Bus 访问 RAM 和 MMIO，不直接识别终端地址或调用文件操作。
- Host 文件操作放在加载器、设备或运行环境适配中；Guest 不直接调用 Host API。
- 先功能正确，再按真实工作负载优化。保持实现简单、明确、可读。
- 注释解释原因、规范依据与不明显的边界条件。
- 工具链、构建工具和正式 ISA 规范版本在对应实现任务中确定。

## 分支与审查

采用轻量 GitHub Flow：main + 短期任务分支 + Pull Request（PR，合并请求）。

- `main` 保存已经验证、可以作为下一步基础的版本；文档阶段不虚构“构建通过”。
- 分支示例：`docs/project-rules`、`feat/cpu-state`、`fix/jalr-alignment`。
- 一个分支和 PR 对应一个可审查能力，可以包含数条细粒度提交。
- 初次仓库建立允许直接在 `main` 写入基础文档与仓库配置；后续功能走任务分支。
- Agent 可以在已授权范围内提交、推送任务分支并创建草稿 PR；功能合入 main 以前由项目所有者审查，已有明确合并授权时按其执行。
- PR 说明当前问题、最终行为、验证证据和限制，不堆叠开发流水账。
- 默认使用 merge commit 合并，保留经整理的学习提交；不默认 squash 掉教学步骤。
- 已发布的提交不默认改写或强推；修正使用新提交。未发布的个人提交可按需要整理。
- 撤销已共享的功能优先使用 `git revert`，使撤销原因留在历史中。

## 提交粒度

一条提交只解决一个逻辑问题，能够单独解释、审查和回退。按逻辑分组，不按文件数或代码行数机械拆分。

合适的例子：

```text
docs(spec): 定义 CPU 初始状态与不变量
feat(cpu): 添加寄存器状态并验证 x0 不可修改
feat(memory): 添加 RAM 字节读写与边界检查
feat(cpu): 实现 ADDI 并覆盖负立即数和回绕
```

避免 `update`、`fix stuff`、`实现整个 CPU`，也避免把头文件和配套实现拆成两个无法构建的提交。无关重构、格式调整和新功能分开。

提交标题采用 `类型(范围): 中文说明`；范围可省略。类型按需使用 `docs`、`feat`、`fix`、`test`、`refactor`、`build`、`ci`、`chore`，不引入自动发布工具。

复杂提交的正文说明为什么修改、行为变化和实际验证结果。每次交付向项目所有者提供提交哈希、该提交解决的问题，以及建议阅读的代码入口。

## 提交前检查

1. 用 `git status` 和 `git diff` 确认变化，只暂存本任务涉及的文件或片段。
2. 检查暂存差异 `git diff --cached`，避免顺手带入无关修改。
3. 运行 `git diff --cached --check` 和当前能力所需的检查。
4. 确认没有凭据、私人资料、构建产物、运行日志和本机磁盘镜像。
5. 提交后检查仓库状态；未通过检查的修改不得声称已完成。

不覆盖用户未提交的工作，不使用清空工作区来解决冲突。确需处理破坏性变化时先给出具体影响。

## 测试、CI 与版本

初始文档阶段检查差异、链接和配置，不设置空测试来制造绿色状态。

第一个可运行 C 模块落地时建立可重复执行的构建和测试，并接入 GitHub Actions。具体编译器和平台在那时确定。测试优先验证外部行为、边界条件和关键不变量，避免只复述实现。

达到有明确验收结果的里程碑后才打版本标签。初始化不宣称 CPU v0.1 已完成，也不提前引入复杂发布流程。

## 参考

- [GitHub Flow](https://docs.github.com/en/get-started/using-github/github-flow)
- [Git：Contributing to a Project](https://git-scm.com/book/en/v2/Distributed-Git-Contributing-to-a-Project)

本项目采用其中的短期分支、审查和逻辑独立提交原则；中文提交格式、保留教学提交是本项目的具体选择。
