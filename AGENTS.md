# Agent 工作准则

开始任务前读取 `README.md`、`CONTRIBUTING.md` 和 `docs/STATUS.md`，检查 Git 状态及相关规格。用户在当前会话中的明确指令优先。

## 当前约定

- 项目由 Agent 实现，与项目所有者逐步讲解和审查。
- Host 核心选用 C17，本机运行优先；此前 TypeScript/Browser 优先的设想已调整。
- 依次完成 INTENTION、SPEC、IMPLE PLAN、IMPLE、VERIFY；纯设计任务停留在设计范围。
- 不因讨论未来能力而提前实现，不把文档中的目标当作已支持能力。
- 关键设计先对齐，已授权范围内的常规实现自主完成，不重复请求相同许可。
- 不生成一整套 CPU 或 OS 后才一次性交付讲解。

## Git 与交付

- 初次仓库初始化之外，在短期任务分支上开发。
- 提交保持逻辑独立、验证通过，代码和配套测试一起提交。
- 正常提交不包含故意失败的测试；测试先写是开发顺序，并非提交顺序。
- 提交前检查暂存差异，运行有关验证，不提交凭据或生成产物。
- 默认保留有教学价值的细粒度提交，不改写已发布历史，不强推。
- 功能合并进入 main 遵守 `CONTRIBUTING.md` 的审查约定。
- 每次交付说明改动、原因、验证、限制和提交哈希，并给出具体的讲解入口。
- 更新当前状态时区分“实现完成”“验证完成”“用户审查完成”，不替用户确认理解。

## 边界

CPU / Bus / Device、Host / Guest、OS / Hardware 保持分离。RISC-V 行为查选定版本的官方规范；不依赖 C 的未定义行为实现指令语义。
