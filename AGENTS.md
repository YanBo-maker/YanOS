# Agent 工作准则

开始任务前读取 `README.md`、`CONTRIBUTING.md`、`docs/STATUS.md` 和相关规格，检查 Git 状态。当前会话中的用户指令优先。

## 协作

- Agent 负责实现和验证，与项目所有者逐步讲解、审查。
- 每次推进一个清晰能力，通过实际输入和状态变化解释原理。
- 按 INTENTION、SPEC、IMPLE PLAN、IMPLE、VERIFY 组织开发。纯设计任务保持在讨论范围内。
- 影响架构、外部行为和学习路线的决定先对齐；已确定范围内的实现自主完成。
- 交付说明改动原因、验证结果、限制、提交哈希和阅读入口。
- 分别记录实现、验证与用户审查状态，理解程度由用户判断。

## 实现与 Git

- Host 采用 C17，本机运行优先；Guest 使用 C 和必要的 RISC-V 汇编。
- CPU / Bus / Device、Host / Guest、OS / Hardware 保持分离。
- RISC-V 行为查选定版本的官方规范，显式处理 C 与 Guest 指令语义的差异。
- 在短期任务分支开发，按逻辑变化提交，配套实现与测试一起验证。
- 遵循 `CONTRIBUTING.md` 的审查流程，保留教学提交及已发布历史。
- 新能力需有当前需求，尚未实现的功能在文档中标为计划。

## 文档与规格的修改权

- 仓库文档（`README.md`、`docs/STATUS.md`、`docs/specs/*`）只有**总审核**角色可以修改。主 Agent 与子 Agent 都不得直接编辑这些文件，包括"只改一个状态行"或"顺手修个错别字"。
- 需要文档变更时，把请求交给总审核角色，写明：位置、原文、拟改文本、依据（命令与输出、规格条款、用户裁定）。由总审核角色整理与执行。
- 规格的**机械性修正**（错别字、失效链接、改名后的标识符、与已交付实现矛盾的过期陈述）由总审核角色直接执行，并在交付报告里逐条列出改前 → 改后与依据；改变行为、接口、数据结构或不变量的**语义性修改**必须走 INTENTION / SPEC / IMPLE 审核，经项目所有者批准后由总审核角色落笔。
- 提交前由总审核角色核对本次改动涉及的文档引用是否自洽（状态行、规格索引、计数、链接，以及历史结论是否被误改），发现不一致先修文档再提交。
- 未经审核改动文档视为**未记录的行为变化**：总审核角色在交付报告里列为 findings，给出 保留 / 回退 / 改写 的裁定建议，由项目所有者决定。
- 历史阶段规格（0005–0008 等标注"本文件保留对应阶段的指令规格"的文件）不得删改其历史结论；已被取代的段落保留原文并标注取代关系。

## 外部监控通道

- 项目所有者授权的外部监控 Agent 会在仓库根写入 `MONITOR_REPORT.md`。它是通道，不是一次性报告：**不要删除**，也不要把其中的结论直接当成事实。
- 总审核角色在每次阶段收口与提交前读取该文件，逐条用命令核实，并把回应写回同一文件（署名、时间、证据）。回应只写核实过的结论。
- 先区分失败结论的来源：`mut-*` / `*-mutation*` 目录或仓库外副本下的日志是变异检查的判据，不是回归结论；只有仓库 `build/` 下默认套件的 CTest 汇总可作为回归证据。
- 未经核实的监控结论不得进入 `README.md`、`docs/STATUS.md` 或规格。

## 文档

- 使用可用的 `style-calibrator` 技能起草和审校读者文档。
- README 介绍项目、实现范围和使用入口；CONTRIBUTING 记录开发流程；STATUS 记录技术进度。
- 本机配置、认证过程和聊天操作记录留在仓库文档之外。
- 直接描述行为和设计理由，按内容安排段落，减少对比套句、口号和重复总结。
- 参考成熟项目的组织方式，保留本项目的事实与术语。
